# Expo/React Native 与 pnpm 配合时，babel-preset-expo 依赖的 @babel/* 插件
# 不会被默认隔离式(.pnpm)布局提升到顶层，导致 Metro 打包报
# "Cannot find module '@babel/plugin-transform-xxx'"。使用 hoisted 布局
# （等价于 npm 的扁平 node_modules）可让 Babel 正常解析这些插件。
node-linker=hoisted
